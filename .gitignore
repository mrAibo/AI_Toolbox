# Ignore local AI state and temp files
.beads/
.agent/memory/session-handover.md
.agent/memory/current-task.md
.agent/memory/.tool-stats.json

# Common build and dependency artifacts
node_modules/
.venv/
target/
dist/
build/
docs/build/

# AI Toolbox client routers (committed as reference templates)
# These files are committed but generated by bootstrap.
# If you need to regenerate them, delete and re-run bootstrap.
# (Note: .gitignore has no effect on already-tracked files.)
